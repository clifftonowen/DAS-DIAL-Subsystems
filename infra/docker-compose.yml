# Local dev orchestration. Supabase itself is hosted (cloud); this runs the API.
services:
  api:
    build: ../backend
    ports: ["8000:8000"]
    env_file: ../backend/.env
    volumes: ["../backend/app:/app/app"]
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
