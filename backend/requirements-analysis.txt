# Offline cohort analysis — the exploration notebook and scripts/ingest_dial_data.py.
#   pip install -r requirements.txt -r requirements-analysis.txt
#
# Deliberately NOT in requirements.txt: the API never reads the workbook. Clustering runs
# once at ingest and its labels are stored in Supabase, so /dashboard/clusters is a plain
# SELECT. Keeping these out means CI installs neither pandas nor Jupyter to run the tests.
# (scikit-learn IS in requirements.txt — ProfilingAlgorithm.cluster() is app code with unit
# tests, even though only the ingest script calls it.)
pandas==2.2.3            # workbook -> DataFrame; the latest-per-student reduction
openpyxl==3.1.5          # pandas' .xlsx engine
matplotlib==3.9.2        # silhouette / elbow curves in the notebook
plotly==5.24.1           # 3D cohort scatter — same library the dashboard uses
jupyter==1.1.1
ipykernel==6.29.5
